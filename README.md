# Cement Mix Calculator

Android app for calculating cement consumption per batch, tracking target scale weights, and marking completed batches with elapsed time between mixes.

## Build

```bat
gradlew.bat assembleDebug
```

The debug APK is generated at `app/build/outputs/apk/debug/app-debug.apk`.
